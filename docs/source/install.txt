.. _install:

Installation
============

To install *minipylib* in your ``site-packages``, do: ::

	cd /path/to/minipylib/directory
	python setup.py install


Documentation
-------------

To build these documentation, cd to ``docs`` directory and do: ::

    make html

HTML docs will be in ``docs/build/html``.


